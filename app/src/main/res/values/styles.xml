<resources>

    <!-- Base application theme. -->
    <style name="Theme.TabbedViewPager.Base" parent="Theme.AppCompat.NoActionBar">
        <!-- status bar color -->
        <item name="colorPrimaryDark">@color/status_bar_color</item>
        <!-- actionbar color -->
        <item name="colorPrimary">@color/app_bar_color</item>
        <!-- window color -->
        <!--<item name="android:windowBackground">@color/pixbg</item>-->
    </style>
    <style name="Theme.TabbedViewPager.Base.Light" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- status bar color -->
        <item name="colorPrimaryDark">@color/status_bar_color</item>
        <!-- actionbar color -->
        <item name="colorPrimary">@color/app_bar_color</item>
        <!-- window color -->
        <!--<item name="android:windowBackground">@color/pixbg</item>-->
    </style>
    <style name="Theme.TabbedViewPager" parent="Theme.TabbedViewPager.Base"></style>
    <style name="Theme.TabbedViewPager.Light" parent="Theme.TabbedViewPager.Base.Light"></style>

</resources>
